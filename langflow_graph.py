def run_graph(medicine, language):
    # Dummy logic for explanation
    description = f"{medicine} is commonly used for pain relief and fever reduction."
    return description if language == "English" else f"{description} (translated to {language})"